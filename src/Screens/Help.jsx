import { Container } from "@mui/material";

export const Help = () => {
    return (
        <Container maxWidth="md">
            {/* need to implement */}
        </Container>
    );
};

export default Help;
